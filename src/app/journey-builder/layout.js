export const metadata = {
  title: "Journey Builder — Design Your Trip | Sattva Sanctum",
  description:
    "Build your perfect journey in 5 steps — choose category, destination, duration, style, and modules. Get a live cost estimate and submit via WhatsApp.",
  alternates: { canonical: "/journey-builder" },
};

export default function JourneyBuilderLayout({ children }) {
  return children;
}
